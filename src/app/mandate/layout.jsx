export const metadata= {
    title: "NCRS - Our Mandate",
    description: "",
  };

export default function AboutLayout({children}){
    return(
        <div>
            {children}
        </div>
    )
}