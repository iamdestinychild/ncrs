

export const metadata = {
    title: "NCRS - ORGANOGRAM",
    description: "",
  };

export default function AboutLayout({children}){
    return(
        <div>
            {children}
        </div>
    )
}