
export const metadata = {
    title: "Department - Policy, Planning And Research (PPR)",
    description: "",
  };

export default function AboutLayout({children}){
    return(
        <div>
            {children}
        </div>
    )
}