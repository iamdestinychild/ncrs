

export const metadata = {
    title: "Administrative & Finance",
    description: "",
  };

export default function AboutLayout({children}){
    return(
        <div>
            {children}
        </div>
    )
}