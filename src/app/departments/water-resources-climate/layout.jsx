
export const metadata = {
    title: "Water Recources & Climate",
    description: "",
  };

export default function AboutLayout({children}){
    return(
        <div>
            {children}
        </div>
    )
}