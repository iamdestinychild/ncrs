
export const metadata = {
    title: "Department - GeoSciences",
    description: "",
  };

export default function AboutLayout({children}){
    return(
        <div>
            {children}
        </div>
    )
}