
export const metadata = {
    title: "Department - Procurement",
    description: "",
  };

export default function AboutLayout({children}){
    return(
        <div>
            {children}
        </div>
    )
}